# Current Feature: Item Create

## Status

In Progress

## Goals

- A "New Item" dialog opens from the existing top-bar button, which is inert today
- Type selector over the five typeable types: snippet, prompt, command, note, link
- Fields render from the selected type:
  - every type: title (required), description, tags
  - snippet / command: content, language
  - prompt / note: content
  - link: URL (required)
- `createItemSchema` with Zod, sharing its field rules with `updateItemSchema`
- `createItem` query in `src/lib/db/items.ts`
- `createItem` server action in `src/actions/items.ts`
- Toast on success, dialog closes, the page behind refreshes to show the new item

## Notes

### What has to be built vs. reused

- **`dialog` and `select` are not installed.** `src/components/ui/` has button, input,
  label, textarea, badge, card, sheet, skeleton, alert-dialog, dropdown-menu, toast,
  avatar, separator. Both come from the shadcn CLI and need **no new npm dependency** —
  Base UI is already installed, exactly as `sheet`, `textarea` and `skeleton` went.
- **`Topbar` is a server component** and its "New Item" `Button` has no handler. The
  client boundary should sit on a new component the top bar renders, not on `Topbar`
  itself — the same call `ItemList` made for the drawer, and it keeps `EnvironmentBadge`
  (a server component reading `DATABASE_URL`) on the server side.
- **Do not build a second field table.** `ITEM_TYPE_EDITABLE_FIELDS` in
  `src/lib/constants/item-types.ts` already says exactly what the spec's field list says
  (snippet → content+language, command → content+language, prompt/note → content,
  link → url). The create form and the create query should both read it, as the edit
  form and `updateItem` already do.
- **Tags are already solved.** `tagsSchema` lowercases, dedupes and caps at 20, because
  `Tag.name` is globally unique. Reuse it; do not write a second normalizer.

### The two columns the edit path never touches

`updateItem` writes an existing row, so it never sets `contentType` or `itemTypeId`.
Create must set both, and neither may come from the client.

- **`contentType`** is a required Postgres enum (`TEXT` / `FILE` / `URL`) saying which
  content column is authoritative. **Nothing in the schema ties it to the item type** —
  a snippet row can be written with `contentType: URL`. Today the mapping exists only as
  one ternary in `prisma/seed.ts`. Both `docs/item-types.md` and
  `docs/item-crud-architecture.md` call for a constant; this feature is where it lands,
  probably `ITEM_TYPE_CONTENT_TYPES` beside the other type tables, derived on the server
  from the chosen type.
- **`itemTypeId`** is a FK, so create resolves the type name to a row. Scope that lookup
  `{ name, isSystem: true }` — `@@unique([name, userId])` means a user could later own a
  custom type also called `snippet`, and `getItemsByType` already pins `isSystem` for
  exactly this reason.

### Decisions to make during implementation

- **Link URL is required here but optional in update.** The edit feature shipped that as
  a known gap ("clearing it leaves the item rendering 'No content'"). If create requires
  it, the two schemas disagree about the same column. Either make update require it for
  links too, or record why create is stricter — do not leave it unnoticed.
- **File and image are absent from the selector**, and that is not an oversight:
  `ITEM_TYPE_EDITABLE_FIELDS` gives them nothing typeable, `fileUrl`/`fileName`/
  `fileSize` come from an upload, and upload is not built. They stay out.
- **Errors inline, success as a toast.** Three features in a row (rate limiting, edit
  mode, delete) deviated from a spec asking for a toast on failure, because a refusal has
  to stay on screen next to the field it is about. Expect the same call here.
- **The action takes `unknown`.** `updateItem` does, deliberately — TypeScript's types
  stop at the Server Action boundary.
- The free tier's 50-item cap is real in the product spec but Pro gating is not enabled
  anywhere yet; out of scope unless asked.
- "New Collection" in the top bar stays inert. Different feature.

### Testing

Server actions and utilities only, per the workflow. The suite is at **127 tests**.
Expect coverage of `createItemSchema` (required title, required URL for links, the
empty-to-null transforms, tag normalization), the `createItem` query (signed-out
returns nothing, the `isSystem` type lookup, which columns each of the five types
writes, the derived `contentType`) and the action (guards, missing type, a 500 that
does not echo the underlying error).

## History

<!-- Keep this updated. Earliest to latest -->

- Initial commit from Create Next App
- Clean up for baseline application
- Lesson 23: Add project context and CLAUDE.md
- Lesson 25: AI workflow & current feature file
- Lesson 27: Dashboard UI prototype
- Dashboard UI Phase 1: shadcn/ui setup, /dashboard route, dark mode default, full-width top bar, sidebar/main placeholders
- Dashboard UI Phase 2: sidebar with type links and item counts, favorite/recent collections, user area; Navigation section with icon-rail collapse, top-bar toggle, and mobile drawer below the top bar
- Dashboard UI Phase 3: main area with stats cards, recent collections grid, pinned items and the 10 most recent items
- Database: Prisma 7 with Neon Postgres, initial schema and migration, driver adapter, system item type seed, and a db:test script
- Seed data: demo user with hashed password, 5 collections and 18 tagged items, plus a db:test that prints the data and asserts its consistency
- Dashboard collections: real collection data from Neon via `src/lib/db/collections.ts`, card accent from the most-used type, icon row for every type in the collection, and a dynamically rendered dashboard route
- Dashboard items: pinned and recent items from Neon via `src/lib/db/items.ts`, real stats card counts, item row icon and border from the item's own type, and a request-cached current user lookup
- Stats & sidebar: system item types with real per-type counts via `getItemTypeCounts`, favorite and recent collections via `getSidebarCollections`, a colored dot for each recent collection's most-used type, a "View all collections" link, and the dashboard layout fetching both for the sidebar
- Pro badge: `PRO_ITEM_TYPES` and `isProItemType` in the item type constants, and a subtle outline `Badge` on the Files and Images rows in the sidebar between the label and the count, hidden in collapsed rail mode; visual marker only, no route gating yet
- Sidebar group collapse on mobile: `SidebarGroup` now treats icon-rail mode as forced-open only on desktop, so the drawer's "Types" and "Collections" headers collapse again after a desktop rail state carries over to a mobile viewport; brings the JS gate in line with the `md:`-prefixed rail styles in `sidebar-styles.ts`
- Auth phase 1 (NextAuth + GitHub): split config keeping Prisma off the edge — `src/auth.config.ts` holds the GitHub provider alone while `src/auth.ts` adds `PrismaAdapter` over the existing prisma singleton, a JWT session strategy, and a session callback copying `token.sub` onto `session.user.id`; `src/proxy.ts` exports a named `proxy` guarding `/dashboard/*` and redirecting signed-out visitors to NextAuth's built-in sign-in page with a `callbackUrl`; `src/types/next-auth.d.ts` narrows `user.id` to a required string. Verified the redirect, the sign-in page and the GitHub OAuth handoff; the full round-trip back to `/dashboard` was left for manual sign-in. Note: `getCurrentUserId` still returns the hardcoded demo user, so the dashboard shows demo data no matter who signs in
- Auth phase 2 (email/password Credentials): `src/auth.config.ts` declares Credentials with an `authorize: () => null` placeholder and exports `CREDENTIALS_PROVIDER_ID` and `credentialFields`, so provider order and field shape are defined once; `src/auth.ts` maps over `authConfig.providers` and swaps that entry for a bcrypt-backed provider whose `authorize` returns `null` (never throws) for bad input, unknown email, GitHub-only accounts with no password, and a failed `bcrypt.compare`. `POST /api/auth/register` validates with Zod, rejects duplicate emails, and catches Prisma `P2002` so a signup race reports 409 rather than 500. `src/lib/validation/auth.ts` shares the rules between the route and `authorize`; its `emailSchema` trims and lowercases *before* validating, because `z.email()` carries its format check at the base and a trailing `.trim()` would never run — review caught `"  padded@test.com  "` returning 400. No migration needed: `User.password` was already in the initial schema; salt rounds match `prisma/seed.ts` at 12. Added `zod` as a direct dependency and gitignored `.playwright-mcp`. Verified registration and every validation path by curl, email normalization end to end, credentials sign-in landing on `/dashboard`, wrong password giving `CredentialsSignin`, and the GitHub handoff still intact. Note: verification initially ran against the **production** Neon branch — `.env`'s `DATABASE_URL` pointed there, which the CLAUDE.md Neon rules do not govern; test users were cleaned up and `.env` repointed to `development` (`ep-green-truth-aylaseez` / `br-still-paper-ay42lb16`), with `.env.production` correctly left on production. Check which branch `DATABASE_URL` resolves to before running write tests through the app, and restart the dev server after changing it since `src/lib/prisma.ts` caches the client on `globalThis`
- Environment badge: `src/lib/db-environment.ts` exposes `getDatabaseEnvironment()`, which parses `DATABASE_URL`, strips a `-pooler` suffix off the host's first label to get the Neon endpoint id, and maps it through `NEON_ENDPOINT_BRANCHES` (`ep-green-truth-aylaseez` = development, `ep-damp-frost-ay6iupf5` = production). Deriving it from the connection string rather than a separate env var is deliberate — a separate var can drift from the real connection, which is the failure being guarded against. An unrecognised endpoint reports its own id instead of guessing, and a missing or unparseable URL returns `null` so it can never crash the layout. `src/components/layout/EnvironmentBadge.tsx` is a server component rendering that label in `Topbar` between the logo and the search box: `destructive` variant with `TriangleAlert` for production, `outline` with `Database` otherwise, endpoint id in the `title`. Returns `null` when `NODE_ENV === "production"`, and only the label crosses to the client — never the connection string. Written because a dev server pointed at production looked identical to one pointed at development. Verified the mapping across pooler and direct hosts, an unknown endpoint, `localhost`, and unset/unparseable URLs; production styling was checked by temporarily forcing the value rather than connecting the app to production
- Auth phase 3 (sign-in, register & sign-out UI): `pages.signIn`/`pages.error` in `src/auth.config.ts` point NextAuth's own redirects at `/sign-in`, and `SIGN_IN_PATH`/`DEFAULT_SIGN_IN_REDIRECT` live there so the proxy, the actions and the pages share one definition. `src/actions/auth.ts` holds the credentials, GitHub and sign-out actions; the credentials action rethrows anything that is not an `AuthError`, because a *successful* sign-in throws `NEXT_REDIRECT` and swallowing it would strand the user on the form. Its error text is deliberately vague — naming which half was wrong turns the page into an oracle for which emails have accounts. `callbackUrl` is sanitised by `safePath` in `src/lib/search-params.ts`, rejecting absolute *and* protocol-relative (`//evil.com`) URLs, and it runs twice: on the page and again in the action, since the hidden field reaches the server from the client. `/sign-in` and `/register` sit in an `(auth)` route group sharing `AuthCard`; both bounce signed-in visitors to `/dashboard`. `RegisterForm` posts to the existing API route rather than an action (a future mobile/CLI client calls the same endpoint), parses with the shared `registerSchema` first so mismatches never leave the browser, then raises a toast and redirects. The toast is Base UI's, chosen over `sonner` because it needs no new dependency and this project has no `next-themes`; `Toaster` *wraps* `{children}` in the root layout — it is a provider, and being at the root is exactly what lets a toast outlive the navigation that follows it. `SidebarUser` became a client component reading the session passed down from the dashboard layout; its old Settings icon button had to go, since a button cannot nest inside the dropdown trigger. `UserAvatar` handles image-or-initials. `/profile` is a placeholder that exists only because typed routes reject a `Link` to a non-existent route, and it is in the proxy matcher. Verified in the browser: guard redirect with `callbackUrl`, wrong password, registration writing to development, email normalisation end to end, initials, dropdown, sign-out, rail mode, the open-redirect rejection, and the toast surviving the redirect (checked in-page, since round-trip latency outran the 5s default — which is why the timeout is 8s). Base UI's `Button` wants a native `<button>`: `render={<Link/>}` logs a console error, so use `buttonVariants()` on the link instead. Note: the pre-delete audit found three accounts created outside this session, so test-data cleanup was scoped to the five `@devstash.test` users by explicit email rather than by a pattern match
- Email verification on register (Resend): registration creates an unverified account and mails a single-use link; credentials sign-in refuses an unverified account; an unverified user can ask for a fresh one. `src/lib/auth/verification-token.ts` holds create/consume/cooldown, using NextAuth's existing `VerificationToken` table — `identifier` is the email — so **no migration was needed**. Only the SHA-256 *hash* is stored: the raw token is a bearer credential, and a leaked dump should not hand over working links. Links last 24h, `createVerificationToken` deletes any outstanding one for the address first so only the newest works, and the claim is a `$transaction` of delete-then-update so two simultaneous clicks cannot both pass. There is no `createdAt` on that table, so the 60s resend cooldown reads a token's age backwards off `expires` (`TTL - n`). A spent token reads as `invalid`, including for a legitimate double-click — once the row is gone nothing distinguishes the cases, and keeping it to tell them apart would mean links outliving their single use; the copy says "invalid or already used… if you have verified, just sign in". The link points at `GET /api/auth/verify-email`, **not** a page, because clicking it mutates and a page component must not mutate while it renders; that route redirects to `/verify-email?status=…`, which also stops the token travelling in the address bar, history and referrer. The `(auth)/verify-email` page is then purely presentational over six statuses, four of which show `ResendVerificationForm`; unknown/missing status falls back to `sent`. The sign-in block throws `EmailNotVerifiedError extends CredentialsSignin` with `code`, deliberately placed **after** `bcrypt.compare` — a wrong password keeps the vague "Invalid email or password", so the specific message only reaches someone who already proved they own the credentials and is not an account-existence oracle. That reverses the old "authorize never throws" rule, so its comment changed. `EMAIL_NOT_VERIFIED_MESSAGE` lives in `src/lib/auth/messages.ts` because the action returns it and the page renders it on NextAuth's own `?error=CredentialsSignin&code=…` redirect; `SignInForm` matches on that one constant to decide whether to show the "Send a new verification link" link. A failed send does **not** roll back the account — the route returns `emailSent` and `RegisterForm` routes to `?status=send-failed`, since the account is real and the resend path is the recovery. `POST /api/auth/resend-verification` answers identically for unknown, already-verified, GitHub-only and just-mailed addresses; only a malformed address gets a distinct 400, as that leaks nothing. Added `resend`, plus `EMAIL_FROM` and `AUTH_URL` (`AUTH_URL` rather than a second app-url var, because NextAuth already reads it and one value cannot drift) to both `.env` and `.env.production`. **The Resend account has no verified domain**, so the sender is the `onboarding@resend.dev` sandbox, which delivers *only* to the Resend account owner — and `.env.production`'s `AUTH_URL` is still localhost, which must change before deploy or production emails carry dead links. `prisma/seed.ts` already set `emailVerified`, so only `scripts/backfill-email-verified.ts` (`db:backfill-verified`) was needed; it grandfathered the 5 pre-existing accounts in, touching only rows where the field is null so it can never un-verify anyone. Verified against development with the real Resend API: registered, pulled the sent message back through `GET /emails/:id` to get the actual link, clicked it (`verified`), clicked again (`invalid`), wrong password → vague / right password → verification message, verified account reaching `/dashboard`, an unreachable address giving `emailSent:false` with the account still created, the cooldown suppressing a duplicate then a real resend going out 79s later and replacing the token, plus garbage/missing token and every resend edge case. Also added `scripts/delete-non-demo-users.ts` (`db:delete-users`): dry-run by default, refuses production via `getDatabaseEnvironment()`, and deletes items *before* item types because `Item.itemType` is `onDelete: Restrict` — leaving that to the user cascade would race a custom type against items still pointing at it; system types (`userId = null`) and shared tags are never touched. Note: `next.config.ts` carries an unrelated pre-existing `devIndicators` change that was deliberately left out of the commit
- Email verification toggle: one switch, `EMAIL_VERIFICATION_ENABLED`, read *only* through `isEmailVerificationEnabled()` in `src/lib/auth/email-verification.ts` — five call sites, one parser, so no two of them can disagree. It exists because Resend still has no verified domain: the `onboarding@resend.dev` sandbox delivers only to the Resend account owner, so every other address could register and then never finish. Parsed **opt-out** — anything but the exact string `"false"` leaves verification **on** — so a missing or misspelt value fails to the secure state rather than silently disabling the check. Server-only: never `NEXT_PUBLIC_`, and the browser is told what happened by a `verificationRequired` field on the register response instead of reading the flag. `RegisterForm` matches on `=== false`, so an older or unexpected response still routes to the check-your-email page. With the flag off, `POST /api/auth/register` stamps `emailVerified` **at create** rather than leaving it null for sign-in to ignore — the difference matters when the flag is turned back on, since stamped accounts keep working and null ones would be locked out; it also skips Resend entirely, so a missing `RESEND_API_KEY` cannot break signup, and reports `emailSent: false` because nothing was in fact sent. The `authorize` gate is `isEmailVerificationEnabled() && !user.emailVerified`, which also lets accounts left unverified by an earlier run sign in. `POST /api/auth/resend-verification` short-circuits to its existing generic response, placed *after* the Zod parse so a malformed address still gets its 400. `GET /api/auth/verify-email` and the `/verify-email` page both redirect to `/sign-in` instead of claiming a token to no effect or showing a status that would be a lie — with the flow off, the only link that can arrive is a stale one, and that account can already sign in. Nothing was deleted or gutted: this is a bypass, every path returns intact when the flag flips, and no migration was needed. **Both `.env` and `.env.production` are gitignored**, so the committed code carries the default and nothing else — production's value has to be set in Vercel's project settings, and leaving it unset means verification stays on, which is the safe direction. Accounts registered while it was *on* and never verified keep `emailVerified: null`: they sign in while it is off and are blocked again if it is turned back on, with `db:backfill-verified` as the escape hatch. Verified both states end to end against development: flag on gave `send-failed` for an undeliverable address, `emailVerified` null and 1 token in the DB, and sign-in blocked with the resend link; flag off gave the "You can sign in now" toast on `/sign-in`, immediate sign-in, the previously-unverified account also reaching `/dashboard`, `emailVerified` stamped with **0 tokens** (proving Resend was never called), both verify-email entry points 307 → `/sign-in`, and resend answering its generic 200 while a malformed address still 400s. Two things learned: Next 16 **refuses to start a second dev server for the same directory**, so testing the other state means restarting the one server — and passing `EMAIL_VERIFICATION_ENABLED` in the process environment is enough, since `@next/env` does not override vars already set, leaving `.env` untouched. Test-data cleanup used `db:delete-users --yes`, which removed every non-demo account on development including the GitHub-linked `driggins@esri.com` (asked for explicitly; it owned no items, and signing in with GitHub recreates it), leaving only `demo@devstash.io`. The unrelated `devIndicators: false` in `next.config.ts` finally shipped, as its own `chore:` commit.
- Forgot password (reset links): a "Forgot your password?" link on `/sign-in`, a `/forgot-password` request page, an emailed single-use link, and a `/reset-password` page that sets the new password. Reset tokens live in NextAuth's existing `VerificationToken` table, so **no migration was needed** — but that table was already owned by the email verification flow, and sharing it is the whole design problem. Verification stores `identifier` as the bare email, so a reset token doing the same would have collided three ways: `createVerificationToken` deletes every row for an address before issuing, so a reset would silently kill a pending verification link and vice versa; `hasRecentVerificationToken` counts any row for the address, so one flow's cooldown would suppress the other's email; and both look a token up by **hash alone**, which is not namespaced, so either flow would happily spend the other's token. The fix is `password-reset:<email>` as the identifier, every reset query scoped to that prefix, and `src/lib/auth/password-reset-token.ts` as a separate module. Testing then surfaced the *reverse* direction, which the spec had missed: a reset token submitted to `/api/auth/verify-email` would have been found by hash and its identifier read as an email — so `consumeVerificationToken` gained a prefix guard too, returning early **without** deleting, because that row is not its to delete. Both directions were checked at module level: each rejects the other's token and neither destroys the other's row. Token handling deliberately mirrors verification — SHA-256 **hash only**, since the raw token is a bearer credential that here grants a password change; issuing replaces any outstanding link; the claim is a `$transaction` of delete-then-update so two simultaneous submits cannot both win; and the 60s cooldown again reads a token's age backwards off `expires` (`TTL - n`), as that table still has no `createdAt`. TTL is **1 hour**, not verification's 24: a forgotten reset link is worth more to whoever finds it. The link points at a **page**, the opposite of the verification link, and for the opposite reason — clicking it only *reads* the token to decide whether to render the form, so nothing mutates during render and a refresh is harmless; the mutation happens on submit, with the token passed by prop and returned in a request body. `POST /api/auth/forgot-password` answers identically for unknown, GitHub-only (`password === null`, nothing to reset) and just-mailed addresses, with only a malformed address getting a 400; it deliberately swallows errors into that same generic 200 rather than a 500, since a send is only ever attempted for an address that *has* an account and a 500 would answer the question the generic response exists to refuse. **Known hole in that:** an existing address does a cooldown query, a token insert and a full HTTP round trip to Resend before replying, while an unknown one returns after a single query — a timing oracle that partly undoes the identical responses. It is pre-existing (`resend-verification` has the same shape) and a real fix means replying before sending; noted, not fixed. A successful reset also stamps `emailVerified` when null, because reading a link in that inbox proves what verification proves and leaving it null would lock the account out the moment `EMAIL_VERIFICATION_ENABLED` is turned on — that flag does **not** gate this flow. `passwordSchema` was extracted from `registerSchema` and `hashPassword` into `src/lib/auth/password.ts`, so the reset and registration cannot drift on either the rule or the cost factor (12, matching `prisma/seed.ts`); the register route now calls it instead of its own `bcrypt.hash`. **Two things browser testing changed.** First, the spec said both new pages bounce signed-in visitors to `/dashboard`, matching the rest of the `(auth)` group — that was wrong, and it is exactly what made the first manual test look broken: a signed-in reader clicking a reset link was dumped on the dashboard with the token unspent and nothing explaining why. A session says nothing about whether a reset link should work. Neither page redirects now, and `/forgot-password` had to follow `/reset-password` because the reset error page's "request a new link" button points straight there. `/sign-in` and `/register` keep their redirects. Dropping `auth()` also made `/forgot-password` build static (`○`). Second, Chrome warned "Password forms should have (optionally hidden) username fields" — without one a password manager has no account to file the new password under — so `checkPasswordResetToken` now returns the account email alongside the status, the page names the account in its description, and a hidden `autocomplete="username"` input carries it; the warning is gone. Naming the account is safe because reaching that page means holding a token, which came from that inbox. The success toast reads "Use your new password next time you sign in", worded for both readers, since a reset completed *while signed in* keeps the session and lands on `/dashboard` via `/sign-in`. Review caught and removed an unused `FORGOT_PASSWORD_PATH` constant; `RESET_PASSWORD_PATH` stays in `auth.config.ts` so the email and the page cannot disagree. No new env vars. Verified against development: identical responses across unknown/known/rate-limited with exactly one namespaced token created and none for the unknown address, the cooldown holding; every schema rejection, garbage token, success, and immediate reuse; expiry from both the read-only check and the consume, with the expired row deleted and the verification row untouched; a token for a deleted account; and in the browser, signed out *and* signed in — form, mismatch caught client-side, toast surviving the redirect, sign-in with the new password, spent/garbage/missing/expired all rendering their states, and `/sign-in` and `/register` still bouncing. Password hash changed at cost 12 and `emailVerified` stamped from null. **Not verified: an actual email.** Every address tested was `@devstash.test`, which the sandbox rejects, so the template, Resend's acceptance and the link inside the message have never been exercised end to end — the Resend account still has no verified domain, so only the account owner's address can receive one. Shipped on that gap knowingly. At completion the development database held a pending `password-reset:` token for the owner address, created outside this session, so the live path may have been exercised separately; no result was observed here, and that token was left alone.
- Profile page (`/profile`): the signed-in account, what it holds, and the two actions that change it. **The prerequisite was the real work.** `getCurrentUserId` still returned a hardcoded `demo@devstash.io` while every function in `src/lib/db/` scoped through it, so the feature was unbuildable on top of it: stats would have reported the demo account's items to whoever was signed in, and *delete account* would have deleted the demo user. It now reads `auth()`. No caller changed — they already treated a null id as "no data", which is exactly the signed-out case — but the dashboard is now **empty for any account that owns nothing**, since the seeded 18 items belong to demo. That is the correct behaviour finally appearing, not a regression. A second helper, `getCurrentUser`, reads the account **from the database rather than the session**, because a JWT carries a snapshot from sign-in time and a profile page must show what is true now; it exposes `hasPassword` rather than the hash, since nothing outside the auth checks should read that. It returns null when the session names a user that no longer exists. **That case bit immediately and is worth remembering:** the browser still held a valid JWT for an account deleted earlier the same day, and `/profile` redirected it to `/sign-in`, which saw a session and redirected to `/dashboard` — a loop with no way to reach the profile *or* sign in again. The fix was to make "signed in" mean "the session names a real user": `/sign-in` and `/register` now ask `getCurrentUser()` instead of `auth()`. That is two files outside the spec's footprint, added deliberately. Deleting an account is a Server Action rather than a route, because signing out is the other half of the job — the cookie keeps working against a deleted row otherwise. It deletes **items, then the user's own item types, then the user**, in one `$transaction`: `Item.itemType` is `onDelete: Restrict` while a user's custom types cascade away with the user, so deleting the user directly races the cascade against that constraint for anyone owning a custom type with items in it. `scripts/delete-non-demo-users.ts` had already solved this; the same order is used here. System types (`userId = null`) and shared `Tag` rows are never touched. Confirmation is the account email **typed out**, not a second button, and the action re-checks it — verified by force-enabling the disabled button in the console and submitting a wrong address, which the server rejected. `POST /api/auth/change-password` verifies the current password with `bcrypt.compare` before writing, reusing `passwordSchema` and `hashPassword`; requiring the current password is what stops someone at a borrowed unlocked screen from taking the account. Unlike the sign-in and forgot-password endpoints it **may be specific** ("Your current password is incorrect") — vagueness there exists to avoid confirming which addresses have accounts, and the caller here has already proved they are this account. The form is shown only when `hasPassword`; a GitHub-only account gets an explanation, and the endpoint refuses independently, since setting a password there would quietly add a second way in. The client also blocks reusing the current password. Nothing navigates after a successful change, so the form is reset by hand rather than leaving the old password sitting in it. `/dashboard` and `/profile` moved into an `(app)` route group sharing the existing dashboard layout, so the profile carries the sidebar and top bar; route groups add no path segment, so **the URLs and the proxy matcher are unchanged** (the layout's `LayoutProps` type changes with its position). `ProfileStats` reuses `getItemTypeCounts` — the same query the sidebar uses, already returning every system type in canonical order with zeros — rather than adding a parallel one. `alert-dialog` came from the shadcn CLI and needed **no new npm dependency**, since Base UI was already installed. Also fixed a latent bug in `SubmitButton`: it spread props *after* `disabled={pending}`, so any caller passing its own `disabled` silently lost the in-flight guard; the two are now combined. No migration. Verified in the browser against development: demo showed 18 items / 5 collections with the breakdown matching the sidebar exactly (4/3/5/0/0/0/6), a fresh account showed zeros throughout with today's join date, wrong current password rejected, same-as-current caught client-side, a real change followed by the old password being refused and the new one accepted, the GitHub-only branch exercised by nulling the password mid-session (which also proved the fresh database read), and a delete of an account first given 3 items, a collection, a custom item type and a shared tag — everything owned went, 7 system types and the shared tag survived, demo's 18 items were untouched, and the session was genuinely gone. **Known gap:** the totals and the per-type breakdown can disagree, because `getItemTypeCounts` lists only system types — that test account read "Items: 3" against a breakdown summing to 2. Unreachable today, since custom types cannot be created in the UI; the one in the test existed only to exercise the delete ordering. Note: development held only `demo@devstash.io` by the end — two accounts present earlier in the session were gone before the delete test and were not removed by it, so the GitHub-only path was exercised by nulling a password rather than through a real OAuth account.
- Rate limiting for auth (Upstash): a sliding-window limiter over Upstash Redis applied to five entry points — credentials sign-in (5 / 15 min, IP + email), `register` and `forgot-password` (3 / hour, IP), `reset-password` (5 / 15 min, IP) and `resend-verification` (3 / 15 min, IP + email). `src/lib/rate-limit.ts` holds one limits table, one `checkRateLimit`, and `rateLimitResponse` for the 429; counters are namespaced `devstash:ratelimit:<name>` so one IP hitting two endpoints does not share a bucket, and an `ephemeralCache` spares Redis a round trip for an identifier already known to be blocked. Added `@upstash/ratelimit` and `@upstash/redis`; **no migration**, and `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` already existed in both `.env` and `.env.production` — both gitignored, so production's values must be set in Vercel or the limiter silently does nothing. **The ordering rule is the load-bearing design decision:** the check runs *after* the Zod parse and *before* any account lookup, and increments for every well-formed request whatever the outcome. Counting only requests that reach a real account would make a refusal proof the address exists — the exact disclosure the generic responses on `forgot-password` and `resend-verification` were built to refuse. Verified both directions: a malformed address 400s five times and never trips the limit (parse really is first), and register's counter advanced across a `201` and two `409`s before refusing. **The review caught a hole that made the headline goal false.** The first implementation put the sign-in limit in `signInWithCredentials`, the Server Action — but that is only the *form's* path to the credentials endpoint. A direct POST to `/api/auth/callback/credentials`, needing nothing but a CSRF token from the public `/api/auth/csrf`, reaches `authorize` without going near the action: measured at **16 consecutive wrong-password attempts, none limited**, every one returning `302 → CredentialsSignin`. Password brute-forcing was untouched by the feature meant to stop it. The fix moves the check into `authorize` in `src/auth.ts`, which *every* caller of the provider goes through, using the `request` NextAuth passes as `authorize`'s second argument. It **moved** rather than being added, or the UI path would spend two tokens per attempt. There is no response to carry a 429 there, so it throws `RateLimitedError extends CredentialsSignin` with `code = RATE_LIMITED_CODE`, mirroring the existing `EmailNotVerifiedError`; the action translates that code for the form and `/sign-in` translates it on NextAuth's own redirect, so a direct-POST attacker and a form user see the same words. Re-verified: attempts 1–5 `code=credentials`, 6–8 `code=RateLimited`, and a different address on the same IP still allowed. `RATE_LIMITED_MESSAGE` deliberately carries **no duration**, unlike the routes' 429 — a NextAuth error code has no room for one, and the only number available is Upstash's `reset`, which is the end of the current sliding sub-window rather than a full window out, so `Retry-After` can under-promise (a 15-minute limit reported "in a minute" under test). **Deviation from the spec:** it said surface the 429 as a toast; all five forms already render `result.error` inline through `AuthError`, and toast is reserved here for success-after-navigation, so a toast would have put this error somewhere different from every other error on the same form. No client code changed at all. Fail-open was proven twice — once accidentally, when `tsx` did not load `.env` and every call returned `remaining=Infinity`, and once deliberately against a dead Redis host, which logged and allowed through rather than throwing. **Known gaps, all deliberate:** keying sign-in by IP + email does not stop credential stuffing (one password against many addresses from one IP), which the spec's own overview names as a threat — closing it needs a second IP-only limit beyond the spec's table; `register` and `forgot-password` at 3/hour by IP alone means one office or CGNAT range gets three signups an hour; `x-forwarded-for` is only trustworthy because Vercel overwrites it, and local testing set it freely to pick buckets, which is exactly what any non-Vercel origin would allow; and `change-password` / `verify-email` were ruled out of scope at load though both are plausible targets. The pre-existing `forgot-password` timing oracle is untouched. Review also trimmed `getClientIp` and `rateLimitMessage` back to module-private once the action stopped using them. Verified against development throughout, with the one account the register test created deleted by exact email — `db:delete-users --yes` was *not* used, since it would also have taken the GitHub-linked `driggins@esri.com` that exists outside this work.
- Items list view (`/items/[type]`): one dynamic route serving all seven system types, making the seven sidebar links that had always 404'd live at once. The slug resolves back to a type name through a new `itemTypeFromSlug()` in `src/lib/constants/item-types.ts`, built by inverting `ITEM_TYPE_SLUGS` rather than by hand, so the two directions cannot drift. **Review caught a real bug in exactly that helper.** The first version used `Object.fromEntries`, and a plain-object lookup walks the prototype chain: `itemTypeFromSlug("constructor")` returned the `Object` constructor, `"__proto__"` returned `Object.prototype`, and `"toString"`/`"valueOf"`/`"hasOwnProperty"` all returned functions — truthy values that the `?? null` fallback never catches. So `if (!type) notFound()` passed a non-type straight into `getItemsByType()` and `ITEM_TYPE_ICONS[type]`, turning a handful of public URLs into 500s while `/items/widgets` looked fine and made the goal appear met. It is a `Map` now, which has no inherited keys; that also dropped the `as Record<string, ItemTypeName | undefined>` cast, so the fix is smaller than what it replaced. Confirmed against all eight probes. `getItemsByType` in `src/lib/db/items.ts` reuses the existing `ITEM_SELECT` and `toDashboardItem`, returns `[]` when `getCurrentUserId()` is null like every other function there, and pins **`isSystem: true`** in the relation filter — type names are unique per `(name, userId)`, so a user could later own a custom type also called `snippet`, and its items do not belong on the system page. Untestable today, since custom types cannot be created in the UI. The page sits in the existing `(app)` group, inheriting the sidebar and top bar at no cost, and is `force-dynamic` like `/dashboard` and `/profile`. **`ItemCard` is the old `ItemRow`, moved rather than duplicated — decided at spec time, not during implementation.** It already drew the type-colored left border and tinted icon tile from the constants tables, and it sets no width and no layout container of its own, so row-versus-card is entirely the parent's decision: the dashboard keeps `space-y-3`, this page uses `grid gap-4 md:grid-cols-2`. Forking would have meant two files sharing four styling tables by hand, the failure those tables exist to prevent. Three changes rode along with the move: the description is `line-clamp-2` instead of `truncate`, since a grid column is narrower and one line loses most of it; it is guarded with `{item.description && …}` because the column is nullable and an empty `<p>` still takes its `mt-1` (the one addition beyond the spec); and an optional `actions` slot sits next to the date for the favorite/pin/delete controls the CRUD feature will add, so that feature need not reopen this file. `src/proxy.ts` gained `/items` and `/items/:path*` — signed out these pages render empty rather than leak, but only because `getCurrentUserId()` returns null, which is a property of the query layer and not a guard. The empty state deliberately carries **no call to action**: there is still no way to create an item, and a button that does nothing is worse than none, so it reads "Notes you add will appear here." rather than "Create your first note". No migration, no new npm dependency, `package.json` untouched. Verified in the browser against development (`ep-green-truth-aylaseez`, confirmed by the environment badge): all seven pages with counts matching the sidebar exactly (4/3/5/0/0/0/6), the empty state on notes, files and images, two columns at 1280px and one at 640px, each type's own accent color, `/items/widgets` returning HTTP 404, the signed-out redirect round-tripping through `callbackUrl` back to `/items/snippets`, and the dashboard rendering unchanged after the component move. **Known gaps, all deliberate:** `getItemsByType` has no `take`, so a Pro account with thousands of items loads all of them — bounded in practice only by the 50-item free tier, and pagination was not in the spec; the `"Not found · DevStash"` string in `generateMetadata` is dead, because `notFound()` hands the title to the not-found boundary (the observed 404 title was plain `DevStash`), but the ternary still needs a branch for type-narrowing; `DashboardItem` is now a misnomer, used by a component and a page that are not the dashboard, left alone rather than churning five files for no behaviour change; and the singular `"1 item"` branch is untested, since no type holds exactly one. A dev-only `TypeError` about a negative performance timestamp appears in the console on the 404 — React's profiling instrumentation reacting to the thrown `notFound()`, absent from production builds. That was **not** confirmed against a production build on purpose: `next start` loads `.env.production`, which points at the production Neon branch. Two process notes: a dev server for this directory was already running from outside the session and Next 16 refuses a second, so testing reused it; and the review step was run without Playwright at request, with the prototype-chain bug proved in plain `node` instead, which was faster and more conclusive than a browser check would have been.
- Vitest for server actions and utilities: one dependency, a Node environment, and no component tests — components are verified in the browser, which is where their real failures show up, and skipping them meant skipping jsdom entirely. `vitest.config.mts` is `.mts` because the config is ESM and the project has no `"type": "module"` (which Next would rather it did not have); `next-auth` and `@auth/core` are inlined through `server.deps.inline`, because Node cannot resolve `next/server` from inside an externalised dependency and the action tests fail to load without it; and the `@/*` alias is written by hand rather than adding `vite-tsconfig-paths` for one line. **`vitest.setup.ts` deletes `DATABASE_URL`**, which matters more than it looks: `src/lib/prisma.ts` throws at import time without it, so an unmocked Prisma import fails loudly instead of opening a connection to Neon. Vitest does not load `.env` the way Next does, so this was already true by accident — stating it makes it a guarantee, and this project's `.env` has pointed at the production branch before. Proved rather than assumed, with a throwaway test asserting that importing `@/lib/prisma` rejects. Tests are colocated as `*.test.ts` and import from `vitest` explicitly rather than enabling globals, so the app's type environment is untouched; they live under `src/`, so `next build` typechecks them too, which is a feature. 63 tests across 7 files in ~1.3s. Several encode regressions already paid for once: the padded `"  padded@test.com  "` that used to 400, the absolute *and* protocol-relative URLs `safePath` must reject, the opt-out parsing where `"flase"` leaves email verification **on**, and the prototype-chain trap in the type lookups. Action tests mock `@/auth`, `@/lib/db/user` and `@/lib/prisma`, and cover what is actually load-bearing: that `callbackUrl` is re-sanitised server-side because the hidden field arrives from the client, that `EMAIL_NOT_VERIFIED_CODE` and `RATE_LIMITED_CODE` are translated while a plain refusal stays vague, that a non-`AuthError` is rethrown so `NEXT_REDIRECT` can complete, that `deleteAccount` re-checks the typed confirmation, and that it deletes items → own item types → user in one transaction. **The suite found a real bug on its first run.** `isItemTypeName` used `name in ITEM_TYPE_ICONS`, and `in` walks the prototype chain, so `isItemTypeName("constructor")` returned `true` and TypeScript then narrowed it to `ItemTypeName`. Same family as the `itemTypeFromSlug` bug caught in the previous review, but in a function that had been there since the dashboard work — and worse, because this one narrows a value read **straight from the database** and feeds `toDashboardItem`, `rankTypes` and `getItemTypeCounts`. Unreachable while only seeded system types exist; the moment custom types ship, a type named `constructor` indexes `ITEM_TYPE_ICONS` to something that is not a component. `Object.hasOwn` now. The test was written first, watched fail, then fixed — which is the whole argument for the suite existing. Docs moved with it: the workflow's **Test** step in `context/ai-interaction.md` changed from "Implement unit testing later" to writing tests and running `npm run test`, with a new Testing section covering colocation, explicit imports, the no-database rule and test-before-fix; `coding-standards.md` carries the conventions; and `CLAUDE.md` lists `test` / `test:watch` again — closing a loop opened earlier the same session, when those two commands were **removed** from CLAUDE.md for advertising scripts that did not exist. `.claude/skills/feature/SKILL.md` gained the `test` action in its table, description and argument hint, registering an `actions/test.md` that was already tracked but unlisted. Note: this work sat uncommitted on `feature/vitest-setup` while the next feature's spec was loaded; the spec was deliberately kept out of these commits so that branching for it would start from a clean `main`.
- Three-column items grid: `/items/[type]` went from `grid gap-4 md:grid-cols-2` to `grid gap-4 sm:grid-cols-2 xl:grid-cols-3` — one column, two, then three. One `className` and a comment; no other file changed. **The breakpoint is the whole feature, and the spec's preferred answer turned out to be wrong.** The spec said `lg:grid-cols-3` to match the dashboard's collections grid and treat this page as the odd one out; `xl` was named only as a fallback "if 230px looks broken — decide by looking, not by arithmetic". It looks broken. Viewport width is not content width here: the sidebar takes ~256px plus 48px of padding, so at `lg` three columns leave about 230px a card, and at that width titles truncated to `Docker Co…` and `shadcn/ui …`, descriptions were cut mid-word, tags stacked one per line and card heights went ragged. The reason collections tolerate `lg` and items do not is that a collection card is laid out **vertically** (name, count, description, icon row) while an `ItemCard` is laid out **horizontally** (icon tile │ content │ date), so it loses its content column first and has a floor the other does not. At `xl` the same three columns get 315px and everything reads. That reasoning lives in a comment at the call site, because the next person to see `xl` here will otherwise "fix" it back to `lg` for consistency. Measured rather than eyeballed, reading `gridTemplateColumns` off the live grid: 639px → 1 col / 576px, 640px → 2 / 288px, 768px → 2 / 220px, 1024px → 2 / 352px, 1280px → 3 / 315px, 1536px → 3 / 400px. The dashboard still stacks its items full-width at 961px, unaffected despite sharing `ItemCard`, because row-versus-grid was always the parent's decision — which is exactly the property that made moving `ItemRow` rather than forking it the right call in the previous feature. One genuine behaviour change beyond the new tier: **640–767px now shows two columns where it showed one**, since the 2-col start moved from `md` down to `sm` to match the dashboard grids. That reads well at ~288px, helped by the sidebar being a drawer below `md` so the cards get the full viewport. **768px is still the worst width on the page** at ~220px a card — the sidebar appears at exactly the width where the second column starts — but that is unchanged, since `md:grid-cols-2` produced two columns there too. A real fix means container queries so the card responds to its own width rather than the viewport's, which also covers the sidebar's icon-rail collapse that no media query can see; ruled out as bigger than this change. **A mistake worth recording:** the first edit put the explanatory comment in as `{/* … */}` immediately inside the ternary's else-branch parentheses, which is not a valid position — that takes a single expression — and it was a hard parse error that 500'd *every* route including `/sign-in`, not just this page. Caught by probing the dev server before trusting it; `//` comments there are fine. The build would have caught it too, just later. The `test` step came back empty exactly as the spec predicted at load time: no server action, no utility, and asserting that a class string contains `xl:grid-cols-3` would only restate the source. Existing suite stayed green at 63 tests throughout. Note: the dev server for this directory had been running since before several branch switches, which is why a stale `Can't resolve '@/components/dashboard/ItemRow'` error sat in its log from the earlier component move — unrelated to this change and already resolved by recompilation.
- Item drawer (`ItemDrawer` + `GET /api/items/[id]`): clicking a card opens a right-side `Sheet` with the item's full record, on the dashboard and the type pages alike — this is the item detail view, and there is no separate item page. `sheet` and `skeleton` came from the shadcn CLI with **no new npm dependency**, Base UI already being installed, exactly as `alert-dialog` had gone. `getItemById` reuses `ITEM_SELECT` and widens it, and puts the ownership check **inside the `where`** (`{ id, userId }`) rather than testing the result afterwards: there is then no branch that could return another user's item, and the caller cannot tell "missing" from "not yours". The route answers **404 for both**, never 403, since a 403 confirms the id is real to anyone enumerating ids. `ItemDetail` is a new type beside `DashboardItem` rather than a widening of it — the list queries read every item a user owns, and a `content` column no card renders would be paid for on every row. **The client boundary sits on a new `ItemList` wrapper, not on `ItemCard`**, which is the design decision the spec called for and it held: the card stays layout-free and prop-dumb, so the dashboard still passes `space-y-3` and the type pages still pass the grid, and neither page's item list crosses to the client. **`ItemCard` became clickable through a stretched overlay button, decided during implementation.** Wrapping the card in a `<button>` is not available: a button may only contain phrasing content, so the `<h3>`, `<p>` and badges cannot live inside one. An `absolute inset-0` button with an `sr-only` label is valid, fully keyboard-reachable, and — the part that matters later — leaves the existing `actions` slot free to hold its own buttons, which nesting would have made invalid HTML. That slot got `relative z-10` so those controls stay clickable above the overlay. The cost is that card text can no longer be selected; the drawer is where you read and copy, so it was accepted. **`item` is passed to the drawer as well as fetched**, which is what makes it feel like a drawer rather than a navigation: the header, type badge, description and tags paint from data the page already has, and only content, collections, language and the updated date wait behind the skeleton. The fetch state is stored **tagged with the id it belongs to** (`{ id, detail, error }`) rather than as separate `detail`/`error` values cleared at the top of the effect — React's compiler lint rejects a synchronous `setState` in an effect body as a cascading render, and carrying the id also makes a stale result recognisable instead of briefly rendering as the new item's. An `AbortController` cancels the previous request on top of that; both were needed, and both were proved by driving a slow first request and a fast second and watching the second's data win. **Only Copy is wired.** It is listed in the spec's action bar and needs no mutation, so it copies `content ?? url ?? fileName ?? title` and raises a toast; favorite, pin, edit and delete all need mutations that belong to the CRUD feature and render `disabled` with a `title`, because a live-looking button that does nothing is the worse lie. **Two layout bugs the browser caught that arithmetic would not have.** First, the drawer stayed **384px** wide however it was styled: `SheetContent`'s own `data-[side=right]:sm:max-w-sm` carries an attribute selector, so a plain `sm:max-w-2xl` loses on specificity — the override has to repeat the `data-[side=right]:` prefix, and the same applies to `w-3/4`, which is now `w-full` below `sm` since a phone should get the whole item rather than a peek at it. Second, at 390px the action bar clipped Edit and pushed the trash icon off-screen entirely; `flex-wrap` sends the right-hand group to its own line instead. **A real bug in a shared component came out of this and shipped as its own `fix:` commit.** The Copy toast fired correctly and sat in the DOM, and was never visible: every overlay in the project is `z-50` — sheet, alert dialog, dropdown — and the toast viewport was `z-50` too, so the later-appended sheet portal won the tie and drew over it. The toast was rendering at x=801 underneath a panel starting at x=529. The viewport is `z-[60]` now, with a comment, because a toast is the topmost transient layer by definition and must outrank modals rather than tie with them; this would have hit *any* toast raised over a modal, not just this one. **Worth remembering about how that was missed:** it was "verified" by reading `[data-slot="toast-title"]` out of the DOM, and presence in the DOM is not visibility — a modal is exactly where those diverge. The conclusive check is `document.elementFromPoint` at the element's centre, which returned the drawer's scroll container before the fix and the toast after it. Compounding it, **MCP round-trip latency outruns the 5s toast timeout**, so click-then-screenshot always photographs an empty screen whether or not a bug exists — the same trap recorded in auth phase 3 and walked into again here; a visual was only obtained by temporarily setting `timeout: 60000`, screenshotting, and reverting. `formatFileSize` moved out of `ItemDrawer.tsx` into a new `src/lib/format.ts` during the `test` step, because testing it in place meant either dragging a Base UI client component into a Node test — against the no-component-tests rule — or leaving the only untested logic in the feature untestable by construction; not `utils.ts`, which is shadcn's own file holding just `cn`. Tests went 63 → **82** across 10 files: 7 on `getItemById` (no query at all when signed out, the scoped `where`, the `constructor`/`__proto__` fallback to `note`), 6 on the route (401 fires *before* the lookup; missing and not-yours both 404; ISO date serialisation, which is why the client revives them; the raw param reaching the query unmangled; a 500 that does not echo the underlying error), and 6 on the formatter, pinning that `1024` reads as **`1.0 KB`** and that `1048575` prints `1024 KB` — a real artifact of rounding after the unit is chosen, recorded rather than hidden. No migration, and `package.json` untouched. Verified in the browser against development (`ep-green-truth-aylaseez`, confirmed by the badge): snippet/prompt/link rendering, Copy landing real content on the clipboard, Enter on a focused card opening the drawer with focus moving inside, the skeleton→loaded transition under a throttled fetch, the error branch replacing the body rather than hanging on a skeleton, and a clean console. Auth was proved over HTTP as well as in unit tests — signed out 401; bogus, `constructor`, `__proto__` and injection-shaped ids all 404 with no 500s; and **a second real account requesting demo's item id got 404**, which needed registering `drawer-probe@devstash.test` and deleting it afterwards by exact email (`db:delete-users --yes` was again *not* used, as it would also take the GitHub-linked `driggins@esri.com`). A throwaway delete script's production guard compared `getDatabaseEnvironment()` to a string when it returns an object, and **failed closed** — worth noting as the guard behaving correctly. **Known gaps, all deliberate:** content is plain `<pre>` with no syntax highlighting, and file/image previews are not built, so a FILE item shows name and size only — `formatFileSize` is therefore unreachable today, since no file items exist and upload is not built; card text is unselectable, the standard cost of the stretched-overlay pattern; and `DashboardItem` is now an even bigger misnomer, still not worth churning files over.
- Item drawer edit mode (`updateItem` action + query, `updateItemSchema`): clicking Edit turns the open drawer into a form rather than navigating anywhere — same panel, one `<form>` spanning the header (where Save and Cancel replace the action bar) and the scrolling body (where the fields are). `textarea` came from the shadcn CLI with **no new npm dependency**, exactly as `sheet`, `skeleton` and `alert-dialog` had. No migration: every column already existed. **The load-bearing decision is that which fields a type may write is a table, not a set of conditionals.** `ITEM_TYPE_EDITABLE_FIELDS` in the item type constants maps each type to the type-specific columns it owns (snippet → content+language, prompt/note → content, command → content+language, link → url, file/image → nothing), and **both** the form and the update query read it: the form to choose which inputs to render, the query to choose which columns to touch. The form deliberately sends *every* field regardless of type and lets the server discard what the type does not own — sending only what was rendered would have made the client the authority, and a hidden input is not a validated one. Proved with a test that walks all seven types asserting exactly which keys reach `item.update`, so a link cannot smuggle in `content` and a snippet cannot set a `url`. `updateItem` in `src/lib/db/items.ts` scopes **both** statements by `{ id, userId }` inside one `$transaction` — the read is needed anyway to learn the type, and the write repeats the filter rather than reusing the id alone, because a `where: { id }` there is one careless refactor away from being the only check that ever ran. Missing and not-yours collapse into one answer (`null` → "This item no longer exists"), matching the detail route's 404-not-403 for the same reason. `toItemDetail` was extracted so `getItemById` and `updateItem` share one mapping. **The stale-card trap the spec flagged at load was real and is the subtlest part of the diff.** View mode used to read title, description and tags from the `item` prop — the card data, which is what makes the drawer open instantly — but `item` is client state held in `ItemList`, so `router.refresh()` re-renders the page *without ever reaching it*. Reading the title from there after a save would show the old one straight back and make a successful write look like it did nothing. View mode now prefers `detail` once the fetch resolves, and the null handling matters as much as the preference: `detail ? detail.description : item.description`, **not** `??`, or clearing a description would fall through to the stale card value. Edit is `disabled` until `detail` arrives, since content, language and URL exist only in the fetched record and a form opened early would start blank and then save those fields away. Edit mode is stored as **which item is being edited** rather than a boolean — the same id-tagging trick the fetch state already used — so opening a different card or closing the drawer simply stops matching and the form resets itself with no effect and no way to reopen into a half-finished edit. The form is seeded at the moment Edit is clicked, not in an effect, because the values already exist and there is exactly one moment they should be read. On success the action's returned record goes **straight into the fetch state**, which is the "refresh without a second fetch" the spec asked for, and `router.refresh()` then catches up the cards behind. **Tags are lowercased, not merely trimmed**, and deduped: `Tag.name` is unique **globally** rather than per user, so "React" and "react" would be two rows nothing joins back together and every user would be typing into the same fragmented table; deduping also stops `connectOrCreate` seeing one name twice in a single nested write. The write is `set: []` then `connectOrCreate` in one `data.tags`, so the list is replaced rather than added to and the shared `Tag` rows are never deleted — orphan cleanup is someone else's job. `updateItemSchema` turns empty and whitespace-only input into `null` rather than `""`, so a nullable column has one representation of "nothing" instead of two, and it accepts `undefined` so a field the form never rendered reads the same as one that was cleared. The URL rule is a union of `""` and `z.url()`, which allows clearing the field while still rejecting a half-typed `http`. **One deviation from the spec:** it said toast on save success *and* error; success is a toast, but errors render inline next to the Save button — a validation message has to stay on screen beside the field it is about rather than vanishing in 5s, and inline is how every other form in this project reports a refusal. Same call, and the same reasoning, as the rate-limiting feature's deviation. The action takes `data: unknown` rather than a declared payload type, because TypeScript's types stop at the Server Action boundary and pretending otherwise is how a parameter type becomes a fake guarantee; the `itemId` is guarded for shape there too. Tests went 82 → **116** across 12 files: 15 on the schema (whitespace-only title, the length caps, empty-to-null, missing-field-equals-cleared, `""` vs `http`, case-collapsing and dedupe, the tag cap counted *after* normalizing), 10 on the query (no transaction at all when signed out, both `where` clauses scoped, no write when the row is not found, the seven-type field gate, `constructor` falling back to note, the tag replace shape) and 10 on the action (the normalized payload reaching the query rather than the raw one, missing/not-yours reported identically, a 500 that does not echo `ECONNREFUSED`). Lint, `tsc --noEmit` and `next build` all clean; `package.json` untouched. **Known gaps, all deliberate:** a link's URL is optional per the spec's literal validation list, so clearing it leaves the item rendering "No content" — arguably it should be required for that one type, and it is a one-line change if so; favorite, pin and delete stay `disabled` pending their own mutations; collections remain display-only in edit mode, shown rather than hidden so there is context to edit against; and the content textarea is still not a code editor. Process note: browser verification was done by the user rather than through Playwright at their request, so no in-session observations of the live form are recorded here — the automated checks above are what this session confirmed directly.
- Delete item (`deleteItem` action + query, `DeleteItemDialog`): the drawer's `Trash2` button, `disabled` with "Deleting is coming soon" since the drawer shipped, now deletes the open item behind an `AlertDialog` that names it. `alert-dialog` was **already installed** from the profile page's delete-account flow, so unlike `sheet`, `skeleton` and `textarea` before it this feature ran no shadcn CLI at all — no new npm dependency, no migration, `package.json` untouched. **The query is `deleteMany`, not `delete`, and that is the one real decision in the data layer.** `delete` throws `P2025` when the row is missing, so distinguishing "already gone" from "the database fell over" would mean catching a Prisma error code; `deleteMany` reports it as a number. It also takes a plain filter, so the ownership check sits in the `where` (`{ id, userId }`) exactly as it does in `getItemById` and both of `updateItem`'s statements — never a test on the result, because a result-side check leaves a branch that could have returned someone else's row. The code reads `count > 0` where the spec said `count === 1`; `id` is the primary key so `deleteMany` can never match more than one, and `> 0` states the question actually being asked rather than leaning on that arithmetic. **No transaction and no relation cleanup**, which is worth stating because it looks like an omission: `ItemCollection.item` is `onDelete: Cascade` and the implicit `_ItemTags` join cascades too, so memberships and tag links go with the row, while the shared `Tag` rows are deliberately left alone — the same rule the edit write follows, orphan cleanup still being someone else's job. A comment says so, so nobody later adds a `tags: { set: [] }` that does nothing. The action mirrors `updateItem`'s guards — signed out, non-string/blank id, query-said-no, thrown — and `"This item no longer exists"` was extracted to a shared `MISSING_ITEM_MESSAGE` constant, the one line of this diff that reaches into `updateItem`; both test files still assert the literal independently, so changing the constant fails both. Missing and not-yours collapse into that single answer for the same reason the detail route answers 404 rather than 403. **The confirmation is one destructive button, not the account dialog's typed email** — typing an address out is the right weight for losing an entire account and far too much for one item, so what makes this deliberate is that the item is named and the button is destructive. Failure renders **inside the dialog** rather than as a toast: that is a deviation from the spec's "toast on success and error", and the same call made twice before (rate limiting, edit mode) — a refusal has to stay on screen next to the thing it is about, and inline is how every other form in this project reports one. The dialog is **controlled** rather than using the stock uncontrolled trigger, since success has to close it programmatically, and `handleOpenChange` returns early while `isDeleting` — Escape and the backdrop both route through it and neither should walk away from a request in flight, which is one step beyond the spec's "disable both buttons". Success sets `open` directly rather than through that guard, which is why it still closes. **On the three traps the spec flagged:** the trigger carries `type="button"` because it genuinely sits inside the drawer's edit `<form>` in the DOM; the dialog's own buttons are portalled out and have no form owner to submit, but carry it anyway rather than resting on that reasoning surviving a refactor. Delete needs nothing from the fetch — the id and title both arrive on the card — so unlike Edit it stays live during the skeleton, and it is absent in edit mode for free, the action bar being replaced by Save/Cancel there. **The `test` step added nothing and instead mutation-checked what existed**, which was the more useful move: changing the `where` to `{ id }` alone failed 2 tests, and removing the signed-out guard failed 1 on both of its assertions. That second one matters more than it looks — without the guard `userId` is `null` and `deleteMany({ where: { id, userId: null } })` is a filter, not a no-op; it deletes nothing today only because `Item.userId` is non-nullable, and the test does not rely on that holding. Tests went 116 → **127** (5 on the query, 6 on the action). One thing tsc proves that no test does: that `deleteMany` returns `{ count }` — the mock asserts that shape, so a wrong guess would pass the suite and break production, and the destructure being typechecked against the generated client is the actual guarantee. **Review caught three things.** A stale error sat on screen during a retry, so a previous "This item no longer exists" rendered directly above a button reading "Deleting…" — two contradictory claims at once; `handleConfirm` clears it now. The description interpolates a title that caps at 200 characters and need not contain a space into a `max-w-sm` popup, so it got `break-words`, which the drawer's own `SheetTitle` already carries for precisely that reason. And replacing the old button dropped its `title` attribute, leaving the trash as the only unlabeled control in a bar where Edit and Copy both show text; `title="Delete"` restored. **Known gaps, all deliberate:** no undo, since a soft-delete column is a schema change and a different feature; favorite and pin stay `disabled` pending their own mutations; no bulk delete and none from the card, `ItemCard`'s `actions` slot being left for favorite/pin/delete to be designed as one row rather than one at a time; and `isDeleting` may stay true briefly past the close because `router.refresh()` is called inside the transition scope, which would make the trigger inert — unreachable in practice, since the drawer is already closing and any refresh settles before another card can be opened. **Browser verification was the user's, at their request, and no results were reported back in-session** — so the nested-overlay z-index tie is *not* confirmed here. That is the one check offline tooling cannot make: the sheet and the alert dialog are both `z-50` and the dialog wins only because its portal is appended later, the identical DOM-order tie that hid the Copy toast until the viewport went to `z-[60]`. What this session confirmed directly is 127 tests, `tsc --noEmit`, `eslint` and `next build`, all clean.
